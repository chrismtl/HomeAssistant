Test suites are separated by concern:

- `unit` for isolated business logic
- `integration` for module and persistence integration
- `hardware` for Jetson device validation with real peripherals
- `e2e` for end-to-end user flows
